---
name: tdd
description: Test-Driven Developmentサイクル（Red-Green-Refactor）で実装を進める
---

# tdd

Test-Driven Developmentのサイクルに従って機能実装を行う。

## 起動時の絶対遵守事項

- 実装コードより先にテストを書く
- テストが失敗することを確認してから実装に進む
- 一度に扱うテストケースは1つに絞る
- masterへの直接コミット禁止、ブランチを切ってから作業すること

## サイクル

### Red: 失敗するテストを書く

1. 実装したい振る舞いを1つ決める
2. その振る舞いを検証するテストを書く
3. テストを実行し、失敗することを確認する
4. 失敗理由が「実装が存在しない」または「期待値と異なる」であることを確認

テストケース名は具体的な振る舞いを記述すること:
- 良い例: 「空配列のとき0を返す」「負の値を渡すとエラーになる」
- 悪い例: 「正しく動作する」「適切に処理される」

### Green: テストを通す最小実装

1. テストをパスさせる最小限のコードを書く
2. ハードコードでも構わない（後でリファクタリング）
3. テストを実行し、全てパスすることを確認
4. 他のテストが壊れていないことも確認

### Refactor: コードを整える

1. テストが通った状態を維持しながらコード改善
2. 重複排除、命名改善、責務分離など
3. 各リファクタリング後にテスト実行
4. テストが壊れたら直前の状態に戻す

## サイクルごとのコミット

- Red-Green-Refactorの各段階で意味のある単位ごとにコミットすることを推奨
- ただしRedだけの状態で共有ブランチにpushしない

## 終了条件

- 要件に対応する全てのテストが書かれ、全てパスしている
- リファクタリング完了
- レビュー準備完了

## アンチパターン

- テストを後付けする
- 実装に合わせてテストを書く（テストが仕様駆動にならない）
- 複数の振る舞いを1つのテストにまとめる
- リファクタリングを省略する
